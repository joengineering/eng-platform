apiVersion: v1
kind: Service
metadata:
  namespace: teams-api
  name: teams-api-service
spec:
  selector:
    app: teams-api
  ports:
    - protocol: TCP
      port: 4200
      targetPort: 8000
  type: ClusterIP