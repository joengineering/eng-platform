apiVersion: v1
kind: Service
metadata:
  name: teams-ui-service
  namespace: teams-ui
  labels:
    app: teams-ui
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: teams-ui