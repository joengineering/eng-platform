# k8s/teams-ui-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: teams-ui-service
  namespace: teams-ui
  labels:
    app: teams-ui
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 443
    targetPort: 443
    protocol: TCP
    name: http
  selector:
    app: teams-ui

